services:
  - type: web
    name: certguard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run_server.py
    healthCheckPath: /docs
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11